# 🚀 הדרכה מהירה - שימוש במערכת החדשה

## מה לעשות עכשיו?

### 1️⃣ התקן את החבילות החדשות
```bash
cd server
npm install
```

זה יתקין את `uuid` ויסיר את `mongoose`.

---

### 2️⃣ הפעל את השרת
```bash
npm start
```

אתה אמור לראות:
```
✅ Created data directory
✅ Created users.json
✅ Created classes.json
✅ Created announcements.json
✅ Created assignments.json
✅ Created events.json
✅ Created media.json
✅ Default admin user created
✅ File-based database initialized
🚀 Server running on port 10000
📁 Data stored in: C:\Users\przyy\Desktop\יאיר\server\data
```

---

### 3️⃣ התחבר למערכת
פתח דפדפן וגש ל: `http://localhost:10000`

**פרטי התחברות:**
- אימייל: `yairfrish2@gmail.com`
- סיסמה: `yair12345`

---

### 4️⃣ בדוק שהכל עובד
1. צור כיתה חדשה
2. הוסף תלמיד
3. צור הודעה
4. העלה קובץ

---

## 📁 היכן הנתונים?

כל הנתונים שלך נמצאים כאן:
```
C:\Users\przyy\Desktop\יאיר\server\data\
├── users.json          ← משתמשים
├── classes.json        ← כיתות
├── announcements.json  ← הודעות
├── assignments.json    ← משימות
├── events.json         ← אירועים
└── media.json          ← מדיה
```

אתה יכול לפתוח כל קובץ ולראות את הנתונים!

---

## 💾 גיבוי מהיר

```bash
# גיבוי
xcopy server\data server\data_backup /E /I /H /Y

# שחזור
xcopy server\data_backup server\data /E /I /H /Y
```

---

## ⚠️ בעיות?

### השרת לא עולה
```bash
# בדוק שהפורט לא תפוס
netstat -an | findstr :10000

# אם תפוס, שנה ב-.env:
PORT=10001
```

### אין תיקיית data
```bash
# פשוט הפעל את השרת - הוא יצור אותה אוטומטית
npm start
```

### רוצה להתחיל מאפס
```bash
# מחק את כל הנתונים
rmdir /s server\data

# והפעל מחדש
npm start
```

---

## ✅ יתרונות המערכת החדשה

1. **פשוט יותר** - אין צורך ב-MongoDB
2. **מהיר יותר** - אין חיבור לשרת חיצוני
3. **קל לגיבוי** - פשוט תעתיק תיקייה
4. **שקוף** - אתה רואה בדיוק מה שמור

---

## 🎉 זהו!

המערכת שלך מוכנה לשימוש!

**שאלות?** קרא את [FILE_STORAGE_MIGRATION.md](FILE_STORAGE_MIGRATION.md)
